# Prevent supply chain attacks via postinstall scripts
# A transitive dependency (plotly.js → gl-text → es6-weak-map → es5-ext)
# runs a malicious postinstall on Linux that installs a crypto miner.
# After running npm install, rebuild native modules manually:
#   npm rebuild better-sqlite3 bcrypt
ignore-scripts=true
